var connection  = require( './connection' );
var eyes        = require( 'eyes' );
var User        = require( '../models/User' );

var userSignup = function( username, email, password, repasswd, callback ) {

  next();

};

module.exports = userSignup;
